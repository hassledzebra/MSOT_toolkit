

function compileToolbox(targetPath)


cd(fileparts(mfilename('fullpath')));
% targetPath = '../../COMPILED/driver/for_testing';
targetPath
mkdir(targetPath);

defaults2JSON;

mcc('-o', 'driver',...
    '-W', 'main:driver',...
    '-T', 'link:exe',...
    '-d', targetPath,...
    '-R', '-logfile,execution.log',...
    '-v', './driver.m',...
    '-a', './+frame',...
    '-a', './+frame/DataFrame.m',...
    '-a', './+filter',...
    '-a', './+recon',...
    '-a', './+unmix',...
    '-a', './+util',...
    '-a', './collatePipelineOutput.m',...
    '-a', './defaultJSONPath.m',...
    '-a', './defaults2JSON.m',...
    '-a', './driveHDF5Writing.m',...
    '-a', './driveRecons.m',...
    '-a', './drivePreprocessing.m',...
    '-a', './driver.m',...
    '-a', './driveUnmixing.m',...
    '-a', './json',...
    '-a', './json/defaults',...
    '-a', './external',...
    '-a', './external/hdf5tools',...
    '-a', './external/msotbeans.jar',...
    '-a', './external/patbeans.jar',...
    '-a', './external/xbean.jar');

system(['mkdir -p ',fullfile(targetPath,'json','defaults')]);
system(['cp -r ./json/defaults ',fullfile(targetPath,'json')]);
end